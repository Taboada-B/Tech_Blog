<main class="container mt-5">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container-fluid">
                 <div class=" navbar" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item">
                            <a class="btn btn-light me-2" role="button" href="/dashboard">Dashboard</a>
                        </li>
                        <li class="nav-item">
                            <a class="btn btn-light" role="button" id="btnLogout" href="/login">Logout</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>

    {{!-- <div class="container mt-5"> --}}
        <h1 class="text-center mb-4">Posts</h1>
        <div class="row justify-content-center">
            {{#each posts}}
            
            <div class="col-md-8 mb-4">
                <div class="card shadow-sm">
                    <div class="card-body">
                        <h2 class="card-title">{{title}}</h2>
                        <p class="card-text m-2">{{content}}</p>
                     <a class="m-2" href="/BlogPost">more details</a>
                     </div>
                </div>
               
            </div>
            
            {{/each}}
        </div>
    {{!-- </div> --}}

    <script src="/js/homepage.js"></script>
    <script src="/js/logout.js"></script>
</main>
